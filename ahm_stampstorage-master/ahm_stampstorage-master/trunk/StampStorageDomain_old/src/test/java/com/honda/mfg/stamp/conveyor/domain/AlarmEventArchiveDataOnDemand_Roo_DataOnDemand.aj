// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.honda.mfg.stamp.conveyor.domain;

import org.springframework.stereotype.Component;

import java.util.List;
import java.util.Random;

privileged aspect AlarmEventArchiveDataOnDemand_Roo_DataOnDemand {

    declare @type: AlarmEventArchiveDataOnDemand:@Component;

    private Random AlarmEventArchiveDataOnDemand.rnd = new java.security.SecureRandom();

    private List<AlarmEventArchive> AlarmEventArchiveDataOnDemand.data;

    public AlarmEventArchive AlarmEventArchiveDataOnDemand.getNewTransientArchivedAlarm(int index) {
        AlarmEventArchive obj = new AlarmEventArchive();
        setLocation(obj, index);
        setAlarmNumber(obj, index);
        setAlarmEventId(obj, index);
        return obj;
    }

     public void AlarmEventArchiveDataOnDemand.setAlarmEventId(AlarmEventArchive obj, int index) {
        Long alarmEventId = new Long(index);
        obj.setAlarmEventId(alarmEventId);
    }

    public void AlarmEventArchiveDataOnDemand.setLocation(AlarmEventArchive obj, int index) {
        Integer location = new Integer(index);
        obj.setLocation(location);
    }

    public void AlarmEventArchiveDataOnDemand.setAlarmNumber(AlarmEventArchive obj, int index) {
        Integer alarmNumber = new Integer(index);
        obj.setAlarmNumber(alarmNumber);
    }

    public AlarmEventArchive AlarmEventArchiveDataOnDemand.getSpecificArchivedAlarm(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        AlarmEventArchive obj = data.get(index);
        return AlarmEventArchive.findArchivedAlarm(obj.getId());
    }

    public AlarmEventArchive AlarmEventArchiveDataOnDemand.getRandomArchivedAlarm() {
        init();
        AlarmEventArchive obj = data.get(rnd.nextInt(data.size()));
        return AlarmEventArchive.findArchivedAlarm(obj.getId());
    }

    public boolean AlarmEventArchiveDataOnDemand.modifyArchivedAlarm(AlarmEventArchive obj) {
        return false;
    }

    public void AlarmEventArchiveDataOnDemand.init() {
        data = AlarmEventArchive.findArchivedAlarmEntries(0, 10);
        if (data== null)
            throw new IllegalStateException("Find entries implementation for 'AlarmEventArchive' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }

        data = new java.util.ArrayList<AlarmEventArchive>();
        for (int i = 0; i < 10; i++) {
            AlarmEventArchive obj = getNewTransientArchivedAlarm(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }

}
