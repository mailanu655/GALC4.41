// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.honda.mfg.stamp.conveyor.domain;

import org.springframework.stereotype.Component;

import java.util.List;
import java.util.Random;

privileged aspect DieDataOnDemand_Roo_DataOnDemand {

    declare @type: DieDataOnDemand:@Component;

    private Random DieDataOnDemand.rnd = new java.security.SecureRandom();

    private List<Die> DieDataOnDemand.data;

    public Die DieDataOnDemand.getNewTransientDie(int index) {
        Die obj = new Die();
        setId(obj, index) ;
        //setDieNumber(obj, index);
        setDescription(obj, index);
        return obj;
    }
     public void DieDataOnDemand.setId(Die obj, int index) {
        Long dieId = new Long(index);
        obj.setId(dieId);
    }

    public void DieDataOnDemand.setDieNumber(Die obj, int index) {
        Integer dieNumber = new Integer(index);
        //obj.setDieNumber(dieNumber);
    }

    public void DieDataOnDemand.setDescription(Die obj, int index) {
        String description = "description_" + index;
        obj.setDescription(description);
    }

    public Die DieDataOnDemand.getSpecificDie(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        Die obj = data.get(index);
        return Die.findDie(obj.getId());
    }

    public Die DieDataOnDemand.getRandomDie() {
        init();
        Die obj = data.get(rnd.nextInt(data.size()));
        return Die.findDie(obj.getId());
    }

    public boolean DieDataOnDemand.modifyDie(Die obj) {
        return false;
    }

    public void DieDataOnDemand.init() {
        data = Die.findDieEntries(0, 10);
        if (data == null)
            throw new IllegalStateException("Find entries implementation for 'Die' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }

        data = new java.util.ArrayList<Die>();
        for (int i = 0; i < 10; i++) {
            Die obj = getNewTransientDie(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }

}
