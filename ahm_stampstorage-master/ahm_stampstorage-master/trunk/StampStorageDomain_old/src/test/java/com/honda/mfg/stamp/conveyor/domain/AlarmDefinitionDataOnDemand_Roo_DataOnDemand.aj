// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.honda.mfg.stamp.conveyor.domain;

import com.honda.mfg.stamp.conveyor.domain.enums.AlarmNotificationCategory;
import com.honda.mfg.stamp.conveyor.domain.enums.SEVERITY;
import org.springframework.stereotype.Component;

import java.util.List;
import java.util.Random;

privileged aspect AlarmDefinitionDataOnDemand_Roo_DataOnDemand {

    declare @type: AlarmDefinitionDataOnDemand:@Component;

    private Random AlarmDefinitionDataOnDemand.rnd = new java.security.SecureRandom();

    private List<AlarmDefinition> AlarmDefinitionDataOnDemand.data;

    public AlarmDefinition AlarmDefinitionDataOnDemand.getNewTransientAlarm(int index) {
        AlarmDefinition obj = new AlarmDefinition();
        setId(obj,index);
        setAlarmNumber(obj, index);
        setLocation(obj, index);
        setName(obj, index);
        setDescription(obj, index);
        setCategory(obj, index);
        setSeverity(obj, index);
        setAutoArchiveTime(obj, index);
        setActive(obj, index);
        setNotificationRequired(obj, index);
        setQpcNotificationRequired(obj, index);
        //setNotification(obj, index);
        return obj;
    }

     public void AlarmDefinitionDataOnDemand.setId(AlarmDefinition obj, int index) {
      obj.setId(new Long(index));
    }
    public void AlarmDefinitionDataOnDemand.setLocation(AlarmDefinition obj, int index) {
        obj.setLocation(index);
    }

    public void AlarmDefinitionDataOnDemand.setAlarmNumber(AlarmDefinition obj, int index) {
        Integer alarmNumber = new Integer(index);
        obj.setAlarmNumber(alarmNumber);
    }

    public void AlarmDefinitionDataOnDemand.setName(AlarmDefinition obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }

    public void AlarmDefinitionDataOnDemand.setDescription(AlarmDefinition obj, int index) {
        String description = "description_" + index;
        obj.setDescription(description);
    }

    public void AlarmDefinitionDataOnDemand.setCategory(AlarmDefinition obj, int index) {
        AlarmNotificationCategory notificationCategory = AlarmNotificationCategory.class.getEnumConstants()[0];
        obj.setNotificationCategory(notificationCategory);
    }

    public void AlarmDefinitionDataOnDemand.setSeverity(AlarmDefinition obj, int index) {
        SEVERITY severity = SEVERITY.class.getEnumConstants()[0];
        obj.setSeverity(severity);
    }
    public void AlarmDefinitionDataOnDemand.setAutoArchiveTime(AlarmDefinition obj, int index) {
       obj.setAutoArchiveTimeInMinutes(Double.valueOf("0"));
    }
     public void AlarmDefinitionDataOnDemand.setActive(AlarmDefinition obj, int index) {
       obj.setActive(true);
    }
     public void AlarmDefinitionDataOnDemand.setNotificationRequired(AlarmDefinition obj, int index) {
       obj.setNotificationRequired(true);
    }
    public void AlarmDefinitionDataOnDemand.setQpcNotificationRequired(AlarmDefinition obj, int index) {
          obj.setQpcNotificationRequired(true);
       }


//    public void AlarmDefinitionDataOnDemand.setNotification(AlarmDefinition obj, int index) {
//        String notification = "notification_" + index;
//        obj.setNotification(notification);
//    }

    public AlarmDefinition AlarmDefinitionDataOnDemand.getSpecificAlarm(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        AlarmDefinition obj = data.get(index);
        return AlarmDefinition.findAlarmDefinition(obj.getId());
    }

    public AlarmDefinition AlarmDefinitionDataOnDemand.getRandomAlarm() {
        init();
        AlarmDefinition obj = data.get(rnd.nextInt(data.size()));
        return AlarmDefinition.findAlarmDefinition(obj.getId());
    }

    public boolean AlarmDefinitionDataOnDemand.modifyAlarm(AlarmDefinition obj) {
        return false;
    }

    public void AlarmDefinitionDataOnDemand.init() {
        data = AlarmDefinition.findAlarmEntries(0, 10);
        if (data == null)
            throw new IllegalStateException("Find entries implementation for 'AlarmDefinition' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }

        data = new java.util.ArrayList<AlarmDefinition>();
        for (int i = 0; i < 10; i++) {
            AlarmDefinition obj = getNewTransientAlarm(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }

}
