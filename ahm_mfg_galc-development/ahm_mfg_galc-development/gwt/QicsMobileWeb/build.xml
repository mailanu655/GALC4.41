<?xml version="1.0"?>
<project name="QicsMobileWeb" basedir="." default="copy-resources">

    <property name="gwt.module.name" value="com.honda.galc.qics.mobile.MobileQics"/>
    <property name="jar.name" value="QicsMobileWeb.jar"/>
    <property name="war.name" value="QicsMobileWeb.war"/>
    <property name="src.dir" location="src"/>
    <property name="build.dir" location="build"/>    
	<property name="war.dir" location="war"/>
    <property name="lib.dir" location="${war.dir}/WEB-INF/lib"/>
    <property name="gwt.client.dir" location="com/honda/galc/qics/mobile"/>
    <property name="gwt.unitCache.dir" location="gwt-unitCache" /> 
	<property name="target.dir" location="../../base-web/src/main/webapp" />
    
    <property name="server.resources.name" value="server_resources"/>
    <property name="server.resources.dir" location="${war.dir}/${server.resources.name}"/>
    <property name="build.server.resources.dir" location="${war.dir}/WEB-INF/classes/server_resources"/> 


	<echo >${basedir}</echo>
    <import file="${basedir}/../../base-library/base/build.xml"/>


    <path id="project.classpath">        
        <fileset dir="${lib.dir}">
            <include name="**/*.jar"/>
        </fileset>
    </path>  
    
    <target name="clean">
        <delete dir="${build.dir}"/>
    	<delete dir="${gwt.unitCache.dir}" /> 
    </target>  
    
    <target name="prepare">
        <mkdir dir="${build.dir}"/>
    </target>

    <!-- Compile the java source code using javac -->
    <target name="compile" depends="prepare">        
        <javac srcdir="${src.dir}" destdir="${build.dir}">
            <classpath refid="project.classpath"/>
        </javac>        
    </target>  
    
   <!-- Package the compiled Java source into a JAR file -->
   <target name="jar" depends="compile">        
       <jar jarfile="${lib.dir}/${jar.name}" basedir="${build.dir}/">
           <!-- Don't wrap any of the client only code into the JAR -->
           <exclude name="${gwt.client.dir}/**/*.class"/>
       </jar>    
   </target>
    
    <!-- Invoke the GWT compiler to create the Javascript files -->
   <target name="gwt-compile" depends="compile">
        <java failonerror="true" fork="true" classname="com.google.gwt.dev.Compiler">
            <sysproperty key="gwt.usearchives" value="false"/>
            <classpath>
                <!-- src dir is added to ensure the module.xml file(s) are in the classpath -->
                <pathelement location="${src.dir}"/>                
                <pathelement location="${build.dir}"/>
                <path refid="project.classpath"/>
            </classpath>
            <jvmarg value="-Xmx512M"/>
            <arg value="${gwt.module.name}"/>
         </java>
     </target>

	<target name="copy-resources" depends="gwt-compile">
        <delete dir="${target.dir}/mobileqics"/>
		<!-- copy GWT compiled javascript files -->
		<copy todir="${target.dir}/mobileqics" overwrite="true">
		    <fileset dir="${war.dir}/mobileqics"/>
		</copy>
	</target>
    
    <!-- Package the JAR file, Javascript, static resources and external libraries into a WAR file -->
    <target name="war" depends="gwt-compile, jar, copy-resources">
        <war basedir="war" destfile="${war.name}" webxml="war/WEB-INF/web.xml">
            <exclude name="WEB-INF/**" />
            <exclude name="${server.resources.name}/**"/>
            <webinf dir="war/WEB-INF/">
                <include name="classes/${server.resources.name}/**" />
                <include name="**/*.jar" />
                <exclude name="**/gwt-dev.jar" />
                <exclude name="**/gwt-user.jar" />
            </webinf>
        </war>
    </target>    
    
</project>

